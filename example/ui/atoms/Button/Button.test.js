// this could instead be a webpack bundle of your client code or something
// the idea is you have javascript that exercises this component, without
// needing to build it into the main bundle (which could be contra to what
// you're overall architecture is trying to do)
console.log('hello world')
